server:
  port: 8088

spring:
  application:
    name: configuracion-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/juanealop/ProyectoMicroservicios
          default-label: main
          search-paths: config-data
          clone-on-start: true
          force-pull: true
